# 작업 규칙

이 저장소에서 사람과 AI가 함께 지키는 규칙이다. **작업을 시작하기 전에 읽는다.**

## 이 문서는 두 이름으로 존재한다

[`AGENTS.md`](AGENTS.md)와 [`CLAUDE.md`](CLAUDE.md)는 **같은 문서다. 한 글자도 달라선 안 된다.**

우리 팀은 Codex와 Claude Code를 섞어 쓴다. 도구마다 읽는 파일이 다르므로, 한쪽에만 규칙을 두면 **다른 도구를 쓰는 사람에게는 그 규칙이 없는 것과 같다.**

- 한쪽을 고치면 **같은 커밋에서** 다른 쪽도 똑같이 고친다
- 두 파일이 다르면 어느 쪽이 맞는지 임의로 정하지 말고 **팀에 묻는다**

```bash
diff AGENTS.md CLAUDE.md && echo OK
```

---

## 이 프로젝트가 무엇인가

**협력업체 탄소데이터 관제탑.** 협력업체가 메일로 보낸 배출 데이터를 시스템이 받아 AI로 읽고, 표준화하고, 적격 여부를 판정한다. 원청 담당자는 검토·확정하고 부적격 건에 피드백을 보낸다.

| | |
| --- | --- |
| Backend | Java · Spring Boot |
| Frontend | Vue.js |
| 인원 | 5명 |
| 기능 | **53개** |

**개발 기간이 짧다. 기능 53개를 다 만들 수 없다.** 아래 모든 규칙은 그 사실에서 나온다.

---

## 무엇을 만드는지는 명세가 정한다

**기준은 하나다** — [`docs/product/REQUIREMENTS.md`](docs/product/REQUIREMENTS.md).

**`1번` ~ `53번`이 이 저장소의 공용어다.** Jira 이슈·브랜치·커밋·PR·테스트가 전부 같은 번호를 가리킨다.

### 시작 전에 세 가지를 찾는다

| 찾을 것 | |
| --- | --- |
| **요구사항 번호** | 이 작업이 53개 중 어느 것인가 |
| **기능 설명 원문** | 그 한 문단이 곧 완료 조건이다 |
| **연결된 기능** | 「→ UC-」와 ⚠️ 표시가 가리키는 곳 |

**요구사항 번호를 못 찾으면 코드를 쓰지 않는다.** 명세에 없는 것을 만들지 않는다 — 팀에 먼저 묻는다.

### 완료 조건은 기능 설명 문장이다

이 명세는 **완료 조건을 문장 안에 넣어 두었다.** 그 문장이 그대로 확인 항목이다.

> 31번 — 「판정이 **적격이고 미등록 부품이 없는 경우에만** 확정할 수 있다」

→ 부적격 건에서 확정 버튼이 **막히는지** 눈으로 본다. 적격 건이 되는 것만 보고 끝내지 않는다.

> 11번 — 「오류가 **하나라도** 있으면 **전체를 등록하지 않는다**」

→ 정상 9행 + 오류 1행을 올려 **9행도 안 들어가는지** 본다.

> 18번 — 「같은 메일은 **Message-ID 기준으로 한 번만** 접수한다」

→ 같은 메일을 두 번 넣어 **두 번째가 안 들어가는지** 본다.

**「되는 것」만 확인하면 절반만 확인한 것이다.** 이 명세의 조건문은 대부분 **막는 쪽**에 있다.

### 아직 정해지지 않은 것을 확정하지 않는다

명세의 **「아직 정해지지 않은 것」** 절을 먼저 본다 — 정렬 기준, 규칙→상태 매핑, UC 문서 부재 등.

**AI에게 시키면 그럴듯하게 채워서 돌려준다.** 채워진 채로 merge되면 아무도 그것이 팀이 정한 것인지 AI가 정한 것인지 모른다.

**막히면 멈추고 묻는다.** 답이 늦으면 PR 본문에 적어 두고 넘어간다 — 조용히 지나가지 않는다.

### 우선순위는 명세가 아니라 Jira 보드에 있다

**명세는 「무엇을 만드는가」만 정한다. 「어느 것을 먼저, 누가」는 보드가 정한다.** 우선순위·담당을 명세에 적으면 5명이 같은 파일의 같은 줄을 고치게 된다 — 보드는 움직이는 기준, 명세는 움직이지 않는 기준이다.

**보드에 올라오지 않은 것을 먼저 만들지 않는다.** 각자 만들고 싶은 것을 만들면 3일차에 **화면은 많은데 하나도 안 이어지는 상태**가 된다.

**최소한 이 줄 하나는 끝까지 이어져야 한다.**

```
18번 메일 수신 → 20번 첨부 저장 → 23번 항목 추출 → 24번 표준화
   → 26번 저장 → 33번·36번 판정 → 29번 검토 목록 → 31번 확정 → 41번 집계
```

---

## AI에게 일을 시킬 때

**이 제품이 AI에게 요구하는 것을, 우리도 AI를 쓸 때 지킨다.**

명세가 다섯 곳에서 같은 말을 한다 — 19번 「미확인」, 20번 「접수 불가」, 22번 「분석 실패」, **24번 「변환할 수 없는 값은 비워 둔 채 사유를 남긴다」**, 25번 「미등록 부품」.

> **모르면 채우지 말고, 비우고 사유를 남긴다.**

### 맥락을 매번 준다

새 대화마다 이 셋을 준다. AI는 지난 대화를 기억하지 못한다.

1. **요구사항 번호와 기능 설명 원문** — 요약하지 말고 그대로
2. **명세의 「상태값」 표** — 안 주면 `ACTIVE`/`PENDING` 같은 새 이름을 지어낸다. 명세는 `협력유지중`·`검토 대기`를 쓴다
3. 관련된 **기존 코드 위치**

### 계획을 먼저 받는다

**바로 코드를 받지 않는다.** 「어떤 파일을 어떻게 바꿀지」를 먼저 받아 읽고 세 가지를 본다.

- 명세에 없는 것을 **스스로 정하지 않았는가**
- 남의 영역(`be`/`fe`) 파일을 건드리지 않는가
- **미결정 항목을 채워 넣지 않았는가**

첫 번째가 걸리면 **거기서 멈추고 묻는다.**

### 돌아간다고 맞는 것이 아니다

**AI가 준 코드가 통과하는 것과 옳은 것은 다르다.**

- **되돌려서 확인한다** — 검사를 넣었으면 **고치기 전 상태로 되돌려 실제로 실패하는지** 본다. 실패하지 않으면 그 검사는 없는 것과 같다. *「통과하는데 아무것도 안 잡는 테스트」가 가장 흔한 함정이다 — 있는 줄 알고 아무도 다시 안 본다*
- **막는 쪽을 확인한다** — 위 「완료 조건」 절 참고
- **모르는 코드를 올리지 않는다** — 설명하지 못하는 코드는 리뷰에서도 설명하지 못한다

---

## Git · Jira

**전체 규칙은 별도 문서를 따른다** — [`docs/GIT_CONVENTION.md`](docs/GIT_CONVENTION.md) · [`docs/JIRA_CONVENTION.md`](docs/JIRA_CONVENTION.md). 여기서는 **AI 작업과 만나는 지점**만 적는다.

| | |
| --- | --- |
| 브랜치 | `feat/be/24-unit-normalize` — `{type}/{domain}/{feature}`, **`{feature}` 앞에 요구사항 번호** |
| 커밋 | `[BE] Feat: CBAM-12 24번 대제목` |
| 흐름 | `feature/*` → `dev` → `main` — **PR은 `dev`로 낸다** |
| Jira | Epic → Task → Subtask · `할 일` → `진행 중` → `완료` · 보드 `CBAM` |

### 커밋에 요구사항 번호를 넣는다

커밋 형식에 이미 **요구사항ID 자리가 있다.** 그 자리에 요구사항 번호를 넣는다.

```
[BE] Feat: CBAM-12 24번 추출값을 표준 단위로 변환한다

- kg → ton, 원본 값을 raw_value 로 함께 보존
- 변환 불가 시 값을 비우고 conversion_note 에 사유 기록 (24번)
```

**Jira 키는 보드와 잇고, 요구사항 번호는 명세와 잇는다.** 하나만 있으면 나중에 무엇을 왜 만들었는지 추적할 수 없다.

### 한 브랜치 = 한 Task = 한 PR

**5명이 병렬로 간다. 범위가 겹치면 그날 하루를 충돌 해결에 쓴다.**

브랜치를 만들기 전에 보드와 `git branch -a`로 **다른 사람이 같은 번호를 잡고 있는지** 본다. 브랜치의 `be`/`fe`가 곧 소유 범위다 — **남의 영역을 고쳐야 하면 먼저 말한다.**

---

## 기록

문서를 많이 만들지 않는다. **두 가지만 남긴다.**

### PR 본문

**merge 뒤에 남는 유일한 기록이다.** 되돌린 접근, 고친 결함, **확인하지 못한 것**을 여기 적는다. 템플릿이 그것을 묻는다.

### 기술 결정 — [`docs/decisions/`](docs/decisions/README.md)

**되돌리기 어려운 선택**만. 라이브러리 도입, 스키마 모양, AI 응답 규격, 미결정 항목의 확정.

*3일 뒤에 누가 「왜 이렇게 했지?」라고 물을 것 같으면 쓴다.*

---

## 안전 규칙

- **비밀값을 커밋하지 않는다.** 메일 계정 비밀번호·AI API 키·DB 접속정보는 `.env`, 저장소에는 `.env.example`만
- **`main` · `dev` 에 직접 push 하지 않는다** — PR은 `dev`로 낸다
- force push·이력 재작성을 하지 않는다
- 남의 커밋을 임의로 되돌리지 않는다

**이 프로젝트는 메일함 접속 정보와 AI API 키를 다룬다.** 한 번 새어 나간 값은 커밋을 지워도 이력에 남고, 그것을 수습할 시간이 없다.

**협력업체 담당자 이메일은 개인정보다.** 실제 주소를 테스트 데이터·스크린샷·이슈 본문에 넣지 않는다.

---

## 막혔을 때

**혼자 30분 넘게 막혀 있지 않는다.** Jira 이슈 댓글로 남긴다 — 개인 메신저로 물으면 기록이 안 남아 같은 질문이 반복된다.
