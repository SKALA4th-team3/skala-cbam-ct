spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: "${DB_URL:jdbc:postgresql://localhost:5432/cbam}"
    username: "${DB_USERNAME}"
    password: "${DB_PASSWORD}"
  jpa:
    hibernate:
      ddl-auto: validate
