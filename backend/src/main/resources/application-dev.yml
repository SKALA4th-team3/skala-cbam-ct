spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:h2:mem:cbam;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
