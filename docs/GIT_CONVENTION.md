# Git Convention

> **출처** — 팀 작성 「Git Convention」. 이 문서는 그 규칙을 저장소 안으로 옮긴 것이다.
> 원본과 이 문서가 다르면 **원본이 맞다.** 발견하면 이 문서를 고치고 팀에 알린다.
> 원본의 `- 폐기` 토글 안에 있던 구버전(SKP-35 표, `S14P21D208` 표)은 옮기지 않았다.

Jira 이슈를 어떻게 등록하고 관리하는지는 [`JIRA_CONVENTION.md`](JIRA_CONVENTION.md)를 본다.

---

## 이 저장소에서 원문과 다르게 정한 것

**원문이 정하지 않았거나, 이 저장소 사정에 맞춰 좁힌 것은 아래 셋뿐이다.** 그 밖에는 원문 그대로다.

| | 원문 | **이 저장소** | 왜 |
| --- | --- | --- | --- |
| 배포 브랜치 | `master` | **`main`** | GitHub 기본 브랜치가 `main`이다. **이름만 다르고 흐름은 원문 그대로다** |
| 도메인 | `be` · `fe` · `ai` | **`be` · `fe`** | AI 호출도 Spring Boot 안에서 일어난다. 커밋 접두어도 `[BE]`·`[FE]` 둘만 쓴다 |
| 브랜치 `{feature}` | 자유 | **`f24-` 처럼 F 번호를 앞에** | 기능 53개를 5명이 나눠 든다. `git branch -a` 만 봐도 누가 어느 F를 잡고 있는지 보여야 겹치지 않는다 |

---

## 🌱 브랜치

### 이름

```
{type}/{domain}/{feature}
```

```
feat/be/f24-unit-normalize
feat/fe/f29-review-list
fix/be/f18-message-id-dedupe
```

| 자리 | 값 |
| --- | --- |
| `{type}` | 커밋 Type을 **소문자로** — `feat` `fix` `docs` `chore` `refactor` `test` … |
| `{domain}` | **`be` 또는 `fe`** |
| `{feature}` | **F 번호를 앞에 붙이고** 작업 기능명을 간결하게 |

**소문자 케밥케이스만 쓴다.**

### 워크플로우

```
  feature/*  (개인 기능 단위 작업)
      │
      ▼
     dev     (통합)
      │
      │  ← dev에서 검증 완료 후에만
      ▼
     main    (실운영 / 데모)
```

- **PR은 `feature/* → dev` 로 낸다.**
- `dev → main` 은 **검증이 끝난 뒤에만.** 검증되지 않은 것을 `main` 에 병합하지 않는다.
- **`main` · `dev` 에 직접 push 하지 않는다.** force push·이력 재작성도 하지 않는다.

### 한 브랜치 = 한 Task = 한 PR

**5명이 병렬로 간다. 범위가 겹치면 그날 하루를 충돌 해결에 쓴다.**

브랜치를 만들기 전에 보드와 `git branch -a` 로 **다른 사람이 같은 F를 잡고 있는지** 본다. 브랜치의 `be`/`fe` 가 곧 소유 범위다 — **남의 영역을 고쳐야 하면 먼저 말한다.**

---

## 📝 커밋

### 형식

```
[BE] Feat: CBAM-12 F-24 추출값을 표준 단위로 변환한다

- kg → ton, 원본 값을 raw_value 로 함께 보존
- 변환 불가 시 값을 비우고 conversion_note 에 사유 기록 (F-24)
```

| 자리 | |
| --- | --- |
| `[BE]` `[FE]` | 담당 영역 |
| `Feat:` | 아래 Type 표 |
| `CBAM-12` | **Jira 이슈 키.** 이게 있어야 Jira ↔ GitHub 연동이 동작한다 |
| `F-24` | **요구사항 ID.** 원문 형식의 「요구사항ID」 자리다 |
| 대제목 | 간결하게 |
| 본문 | **반드시 줄바꿈하여** 항목별로. **왜 / 무엇을 / 어떻게** |

**Jira 키와 F 번호 둘 다 넣는다.** 키는 보드와 잇고, F 번호는 명세와 잇는다 — 하나만으로는 나중에 무엇을 왜 만들었는지 추적할 수 없다.

### Type

| Type | 사용 시점 |
| --- | --- |
| Init | 프로젝트 초기 세팅 |
| Add | 새로운 파일 추가 |
| Delete | 파일 삭제 |
| Feat | 새로운 기능 추가 / 기능 수정 |
| Fix | 버그 수정 |
| Build | 빌드 관련 설정 수정 |
| Chore | 기타 설정 수정 (.gitignore 등) |
| Docs | 문서 및 주석 수정 |
| Style | 코드 스타일 / 포맷 수정 |
| Refactor | 기능 변경 없는 리팩토링 |
| Test | 테스트 코드 추가/수정 |
| Release | 버전 릴리즈 |
| Rename | 파일/폴더 이름 변경 |
| Readme | README 수정 |
| Comment | 주석 관련 작업 |

---

## 🔀 Pull Request

### 제목

**커밋 제목과 같은 형식을 쓴다.**

```
[BE] Feat: CBAM-12 F-24 추출값을 표준 단위로 변환한다
```

제목만 보고도 **무엇을 했는지 알 수 있게** 쓴다.

### 본문

[`.github/pull_request_template.md`](../.github/pull_request_template.md) 가 자동으로 붙는다. 원문 템플릿의 세 절(**한 줄 요약 · 세부 내용 · Issue 번호**)에 **확인 항목**을 더한 것이다.

**Jira 이슈 키를 제목 또는 본문에 반드시 넣는다.** 연동 앱이 그것으로 이슈를 찾는다.

템플릿의 **「확인하지 못한 것」과 「명세에 없어서 임의로 정한 것」을 비워 두지 않는다.** 없으면 「없음」이라고 적는다 — 비어 있으면 다음 사람은 「했다」인지 「못 했다」인지 알 수 없다.

---

## 커밋 전에 한 번

```bash
diff AGENTS.md CLAUDE.md && echo OK
```

두 파일은 같은 문서다. 한쪽을 고쳤으면 **같은 커밋에서** 다른 쪽도 고친다.
